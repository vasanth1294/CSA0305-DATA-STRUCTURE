#include<stdio.h>
int factorial(int n)
{
if(n==0)
return 0;
else if(n==1)
return 1;
else
return n*factorial(n-1);
}
int main()
{
int n,i;
clrscr();
printf("\nenter the limit for factorial:");
scanf("%d",&n);
printf("\nfactorial is %d",factorial(n));
getch();
return 0;
}
